{
  "name": "despegar-flightsearch-automation",
  "version": "1.0.0",
  "description": "Project in Cypress, for the parameterized automation of the search for flights on the Despegar site",
  "main": "index.js",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "./node_modules/.bin/cypress run --env",
    "cy:run:headed": "./node_modules/.bin/cypress run --headed --env"
  },
  "author": "Simon Gerula",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^8.2.0"
  }
}
